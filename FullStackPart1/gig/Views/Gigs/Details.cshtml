@model GigHub.Core.ViewModels.GigDetailsViewModel

@{
    ViewBag.Title = "Details";
}

<div id="DivFollow">
    

<h2>@Model.Gigs.Artist.Name
    @if (User.Identity.IsAuthenticated)
    {
        if (Model.IsFollowing)
        {
            <button data-Gigs-id="@Model.Gigs.ArtistId" class="btn btn-info js-toggle-Follow">Follow</button>
        }
        else
        {   <button data-Gigs-id="@Model.Gigs.ArtistId" class="btn btn-default js-toggle-Follow">Following</button> }

    }
  
</h2>
</div>
<span>
    <p>Performing At @Model.Gigs.Venue on @Model.Gigs.DateTime.ToString("f")</p>
</span>

    @if (User.Identity.IsAuthenticated && Model.IsAttending)
    {
        

             <p>You are going to this event</p>
   

    }
   
@section scripts
{

    <script>

        $(document).ready(function () { GigDetailsContoller.init();});



    </script>





}
